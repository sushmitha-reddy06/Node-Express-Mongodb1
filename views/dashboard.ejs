<%-include('header')-%>

<div class="board">
<div class="text-center center-div mt-5" id="login">
    <div class="container w-25 border py-5 mt-5">
    <h3>Welcome to Express Dashboard</h3>
    <% if (locals.user) { %>
        <h5><%= locals.user.firstname %> <%= locals.user.lastname %></h5>
      <% } else { %>
        <p>User</p>
      <% } %>
    <a href="/logout">Logout</a>
    </div>
</div> 
</div>

<%-include('footer')-%>
